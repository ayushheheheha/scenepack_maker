export default function Editor() {
  // Placeholder — the editing UI is built in a later phase.
  return (
    <div className="flex h-full w-full items-center justify-center bg-bg text-white">
      <p className="text-sm text-white/50">Editor — coming in a later phase</p>
    </div>
  )
}
